        <nav class="navbar navbar-light">   
            <img alt="Chirper" src="./assets/images/Chirper.png" class="custom-img">


//{{#if loggedIn}} 
    <div class="card w-75 mx-auto custom-hp-bg text-center" style="width: 18rem;">
	    <div class="card-body">
    		<h3 class="card-subtitle mb-2">~ You are not Signed in ~</h3>
		    <br><br>
		    <br><br>
		    <button type="submit" id="signup-initial" class="btn custom-btn font-weight-bold px-md-5">Sign Up</button>
            <button type="submit" id="signin-initial" class="btn custom-btn font-weight-bold px-md-5">Sign In</button>
	    </div>
    </div>
//{{/if}}


            {{!-- {{#if loggedIn}} // Will be implemented once authentication is finished --}}
            <div>
                <button type="button" id="newchat" class="btn btn-outline-secondary">New Chat</button>
                <button type="button" id="logout" class="btn btn-outline-dark">Log Out</button>
            </div>
            {{!-- {{/if}} // Will be implemented once authentication is finished --}}
        </nav>


        <div class="container">
            <div class="row justify-content-center">
                <div class="col-sm-8">
                    <h2>Welcome {{{user}}}</h2>

                    {{#each user  as |Administrator|}}
                        {{> chats}}
                    {{/each}}

                </div>
            </div>
        </div>

        {{!-- {{#if loggedIn}} //Will be implemented once authentication is finished --}}
            <script>"./js/logout.js"</script>
        {{!-- {{/if}} //Will be implemented once authentication is finished --}}


    <div class="container">
        <div class="row justify-content-center">
            <div class="col-sm-8">
                <h2>Welcome {{{user}}}</h2>

                {{#each user  as |Administrator|}}
                    {{> chats}}
                {{/each}}

            </div>
        </div>
    </div>

        
    <script>"./js/logout.js"</script>
        

//{{!-- {{#if loggedIn}} //Will be implemented once authentication is finished --}}
//    <script>"./js/logout.js"</script>
//{{!-- {{/if}} //Will be implemented once authentication is finished --}}

